export default function ComparePanel() {
  return (
    <div className="rounded-md border border-dashed border-slate-300 bg-slate-50 p-4 text-sm text-slate-600">
      County comparison workspace is structured for a future release. The API already exposes common metrics and rankings needed for side-by-side comparisons.
    </div>
  );
}
